apiVersion: v1
kind: ConfigMap
metadata:
  name: "terraform-s3-lambda-${{values.name}}-tf-cm"
  namespace: flux-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
data:
  ${{ values.tfVars | dump }}